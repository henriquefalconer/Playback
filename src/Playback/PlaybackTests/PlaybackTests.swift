import XCTest
@testable import Playback

/// Base test case for Playback unit tests
class PlaybackTests: XCTestCase {
    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }

    func testExample() {
        XCTAssertTrue(true, "Placeholder test to verify test target is working")
    }
}
